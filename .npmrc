; The plugin installer runs `npm install` inside the plugin cache directory.
; Nothing here is needed at runtime — typescript and @types/node are only for
; `npm run typecheck` — so keep them out of an installed copy.
; Developing on this repo: `npm install --include=dev` (or `npm run setup`).
omit=dev
